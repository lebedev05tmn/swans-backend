import { Socket } from 'socket.io';

export const socketDisconnect = async (socket: Socket) => {
    // const user = await profileRepository.findOneBy({
    //     socket_id: socket.id,
    // });
    // if (user) {
    //     user.socket_id = null;
    //     await profileRepository.save(user);
    // }
};
